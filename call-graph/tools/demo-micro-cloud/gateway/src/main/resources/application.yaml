server:
  port: 18550

spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      routes:
        - id: service-a
          uri: lb://service-a
          predicates:
            - Path=/test/{int:\d+}

        - id: service-a
          uri: lb://service-a
          predicates:
            - Path=/test/{str:[a-zA-Z]+}

        - id: service-b
          uri: lb://service-b
          predicates:
            - Path=/service-b/test-service-b/**
          filters:
            - StripPrefix=1